# 生信机器学习与深度学习入门指南

> 写给生物信息学初学者的 ML/DL 全景地图 —— 从零基础到能看懂论文、跑通第一个模型。

---

## 目录

1. [为什么生信需要机器学习？](#1-为什么生信需要机器学习)
2. [机器学习核心概念速览](#2-机器学习核心概念速览)
3. [核心算法详解](#3-核心算法详解)
4. [在生信领域的经典应用](#4-在生信领域的经典应用)
5. [应用扩展：多组学与多模态融合](#5-应用扩展多组学与多模态融合)
6. [在单细胞领域的应用](#6-在单细胞领域的应用)
7. [深度学习在生信中的特殊地位](#7-深度学习在生信中的特殊地位)
8. [初学者学习路线图](#8-初学者学习路线图)
9. [常用工具与资源](#9-常用工具与资源)
10. [常见误区与避坑指南](#10-常见误区与避坑指南)

---

## 1. 为什么生信需要机器学习？

### 一个生动的开场故事

想象你是一名侦探，手里有 **20,000 个人类基因** 的表达数据，你需要从这 20,000 个"嫌疑人"中找出哪些基因导致了某种癌症。传统的做法是逐个排查 —— 这就像在大海里捞针。而机器学习就像给你配了一个**超级助手**，它能同时审视所有基因之间的复杂关系，自动找出真正的"罪犯"。

### 生信数据的三大特点

| 特点 | 说明 | 举例 |
|------|------|------|
| **高维度** | 特征数远大于样本数（p >> n） | 2 万个基因，却只有几十个病人样本 |
| **高噪声** | 生物系统本身充满随机性 | 同一组织、同一基因，不同测序批次结果不同 |
| **高相关性** | 特征之间存在复杂互作网络 | 基因 A 和基因 B 可能协同调控基因 C |

传统统计学方法（如 t 检验、线性回归）在面对这种数据时常常捉襟见肘，而机器学习天然适合处理高维、非线性、强相关的数据 —— 这就是为什么生信需要 ML/DL。

---

## 2. 机器学习核心概念速览

### 2.1 三种学习范式

```
机器学习
├── 监督学习 (Supervised Learning)    ← 数据有标签，如"患病 vs 健康"
├── 无监督学习 (Unsupervised Learning)  ← 数据无标签，让模型自己找规律
└── 半监督学习 (Semi-supervised)      ← 少量有标签 + 大量无标签数据
```

### 2.2 四个核心概念

| 概念 | 通俗解释 | 生信中的例子 |
|------|----------|-------------|
| **特征 (Feature)** | 输入给模型的每个变量 | 每个基因的表达量 |
| **标签 (Label)** | 我们想预测的东西 | 病人是否患病、生存时间 |
| **训练/测试集** | 用一部分数据学习，另一部分考试 | 用 80% 的样本训练，20% 来检验效果 |
| **过拟合 (Overfitting)** | 模型"背题"而不是"学会" | 在训练数据上准确率 99%，在新数据上只有 60% |

### 2.3 分类 vs 回归

- **分类 (Classification)**：预测离散的类别。例如：判断一个肿瘤是良性还是恶性。
- **回归 (Regression)**：预测连续的数值。例如：预测病人的生存时间（月）。

---

## 3. 核心算法详解

### 3.1 监督学习算法

#### ① 逻辑回归 (Logistic Regression) —— "入门的第一个模型"

尽管名字里有"回归"，但它是一个**分类**算法。

- **核心思想**：给每个特征分配一个权重，加权求和后通过 Sigmoid 函数映射到 0~1 之间，代表属于某类的概率。
- **生动类比**：就像给一个病人的各项指标"打分"——基因 A 高表达 +3 分（危险），基因 B 高表达 -2 分（保护），最后总分换算成患病概率。
- **生信应用**：构建基因表达签名（gene signature）来预测疾病亚型。
- **优点**：可解释性强，你能看到每个基因的贡献方向和大小。
- **Python 一行代码**：`from sklearn.linear_model import LogisticRegression`

#### ② 随机森林 (Random Forest) —— "生信中的瑞士军刀"

- **核心思想**：构建很多棵决策树，每棵树只看一部分基因、一部分样本，最后投票决定结果。
- **生动类比**：找 100 个医生来会诊。每个医生只拿到部分检查报告（随机选的特征），也只看了部分病人档案（随机选的样本）。最后少数服从多数 —— 这就是随机森林。
- **为什么适合生信**：
  - 天然处理高维数据（基因数 >> 样本数）
  - 自动捕捉基因之间的交互作用
  - 能输出特征重要性排名（哪些基因最重要）
- **缺点**：模型较大，不太适合部署到轻量级场景。
- **Python 一行代码**：`from sklearn.ensemble import RandomForestClassifier`

#### ③ 支持向量机 (SVM) —— "寻找最优分界线"

- **核心思想**：在两类样本之间找到一条"最大间隔"的分界线，使得两类离分界线都尽可能远。
- **生动类比**：在癌症和正常样本之间画一条线，要求这条线离最近的癌症样本和最近的正常样本都尽可能远 —— 这样新样本来了也不容易被误判。
- **核技巧 (Kernel Trick)**：当数据在低维空间分不开时（比如基因调控常常是非线性的），SVM 通过核函数把数据"投射"到高维空间，在高维空间中找一个线性分界面。想象把二维平面上混在一起的两种颜色的豆子，拎起其中一个维度变成三维，在三维中用一个平面轻松分开。
- **生信应用**：基因表达分类、蛋白质功能预测。
- **Python 一行代码**：`from sklearn.svm import SVC`

#### ④ XGBoost / LightGBM —— "Kaggle 竞赛之王"

- **核心思想**：梯度提升树 —— 一棵树接一棵树地学习，每次新树专注于修正前面所有树的错误。
- **生动类比**：你第一次考试只考了 60 分，分析错题后第二次专门补这些错题考了 75 分，第三次再补剩下的错题考了 85 分……每次都在修正之前的不足。XGBoost 就是把这种"查漏补缺"做到了极致。
- **生信优势**：在表格类型数据（如临床指标 + 基因表达矩阵）上几乎无敌，速度快，内置正则化防止过拟合。
- **Python 一行代码**：`from xgboost import XGBClassifier`

---

### 3.2 无监督学习算法

#### ① K-Means 聚类 —— "最朴素的分组方法"

- **核心思想**：指定 K 个中心点，把每个样本分配到最近的中心点，迭代更新直到稳定。
- **生动类比**：班级里要分 3 个学习小组，先随机指定 3 个组长位置，大家站到离自己最近的组长旁边，然后组长移动到组员的中心位置，大家重新站队……重复几轮后分组就稳定了。
- **生信应用**：基因共表达模块发现、样本亚型初步探索。
- **局限性**：需要预先指定 K；假设簇是圆形的；对离群点敏感。

#### ② 层次聚类 (Hierarchical Clustering) —— "生信中最常见的图"

- **核心思想**：自底向上（或自顶向下）逐层合并（或拆分）样本，形成树状结构。
- **生动类比**：就像整理家族谱系 —— 先找到最相似的两个人合并成一个"小家庭"，再不断把小家庭合并成大家族，最后形成一棵完整的家族树。
- **为什么生信中无处不在**：你看到的所有基因表达热图，其行和列的侧边树状图就是层次聚类的结果。
- **Python**：`from scipy.cluster.hierarchy import linkage, dendrogram`

#### ③ PCA (主成分分析) —— "降维神器"

- **核心思想**：找到数据方差最大的方向（主成分），把数据投影到这些方向上，用少数几个维度表示原本的高维数据。
- **生动类比**：你要给一个三维的雕塑拍一张照片，从正面拍能看到最多细节，从侧面拍其次，从顶面拍最差。PCA 就是自动找到"最佳拍摄角度"，把 20,000 维的基因数据压缩到 2-3 维，让你能画出来看。
- **生信应用**：样本间的整体相似性可视化、批次效应检测、数据降噪。
- **Python 一行代码**：`from sklearn.decomposition import PCA`

#### ④ t-SNE / UMAP —— "可视化之王"

- **核心思想**：在降维时尽量保持"近邻关系"——原本空间中相近的点，在二维图中也要挨着。
- **生动类比**：PCA 像从远处拍全景（保全局结构），t-SNE/UMAP 像用微距镜头拍局部细节（保局部邻里关系）。
- **单细胞标配**：没有 t-SNE/UMAP 图，单细胞论文几乎发表不了。它能把几万个细胞的转录组画成一张散点图，不同类型的细胞自然聚成不同的"岛屿"。
- **注意**：t-SNE/UMAP 只能用于可视化和探索，**不能**用图中距离做定量比较。簇之间的距离和大小没有实际意义。
- **Python**：`from sklearn.manifold import TSNE` / `import umap`

---

### 3.3 深度学习算法

#### ① 全连接神经网络 (MLP / DNN) —— "深度学习的基本单元"

- **核心思想**：多层神经元，每层对上一层进行非线性变换。
- **生动类比**：第一层识别基因的高/低表达，第二层识别"这几个基因同时高表达"的模式，第三层识别更复杂的疾病特征模式……层级越高，学到的概念越抽象。
- **生信应用**：药物响应预测、基因表达缺失值填补。

#### ② 卷积神经网络 (CNN) —— "从序列和图像中提取模式"

- **核心思想**：用滑动窗口扫描输入，检测局部模式（motif）。
- **生动类比**：给你一长串 DNA 序列，你拿一个长度为 6 的"放大镜"从左滑到右，每次看 6 个碱基，识别出其中的调控元件 motif。CNN 就是把这个过程自动化了。
- **生信应用**：
  - DNA 序列中识别转录因子结合位点
  - 蛋白质二级结构预测
  - 组织病理切片图像分类（判断肿瘤类型）
- **Python**：`from tensorflow.keras.layers import Conv1D, Conv2D`

#### ③ 循环神经网络 (RNN / LSTM / GRU) —— "处理序列数据"

- **核心思想**：处理序列数据时，网络的输出不仅取决于当前输入，还取决于之前见过的内容（有"记忆"）。
- **生动类比**：读蛋白质序列 "M-A-K-L-V-F..." 时，读到第 5 个氨基酸 V 时，你还记得前面是 MAKL，这样就能判断 V 所处的位置是不是跨膜区域。
- **生信应用**：DNA/RNA/蛋白质序列分类、基因表达时序预测。
- **已被取代的趋势**：在序列建模上，Transformer 正在逐步替代 LSTM。

#### ④ Transformer / 注意力机制 —— "改变游戏规则的技术"

- **核心思想**：不再依次处理序列，而是一次性看整个序列，通过"注意力权重"决定序列中哪些位置之间关系更密切。
- **生动类比**：读一句话时，你不需要逐个单词地线性理解 —— "苹果"这个词的出现会让你格外注意前面的"吃"和"红"，而不是远处的"汽车"。注意力机制就是让模型学会这种"注意什么"的能力。
- **革命性意义**：
  - 可并行训练（不像 RNN 必须串行）
  - 能捕捉长距离依赖（基因 A 调控基因 Z，中间隔了几万个碱基）
  - 衍生出了 AlphaFold、DNABERT、scGPT 等颠覆性模型

---

## 4. 在生信领域的经典应用

### 4.1 基因组学

| 任务 | 常用方法 | 具体例子 |
|------|----------|----------|
| 变异致病性预测 | RF, XGBoost, CNN | 判断一个 SNP 突变是良性还是致病 |
| 基因调控元件识别 | CNN, Transformer | 从 DNA 序列中找启动子、增强子 |
| 可变剪接预测 | Bayesian NN, RF | 预测某个外显子是否会被包含 |
| 染色质状态标注 | HMM, CRF, CNN | 将基因组分段标注为启动子区/增强子区/沉默区等 |

### 4.2 转录组学

| 任务 | 常用方法 | 具体例子 |
|------|----------|----------|
| 差异表达基因筛选 | DESeq2 + ML 后筛选 | 找出疾病 vs 对照的关键基因 |
| 分子亚型分类 | RF, SVM, NMF | 将癌症患者分为不同分子亚型，指导精准治疗 |
| 基因共表达网络 | WGCNA, K-Means | 找出协同表达的基因模块 |
| 药物响应预测 | XGBoost, DNN | 根据基因表达预测患者对化疗是否敏感 |

### 4.3 蛋白质组学

| 任务 | 常用方法 | 具体例子 |
|------|----------|----------|
| 蛋白质结构预测 | AlphaFold (Transformer) | 从氨基酸序列预测 3D 结构 —— 2024 年诺贝尔化学奖 |
| 蛋白质功能预测 | GNN, CNN | 判断一个蛋白是不是激酶、转录因子 |
| 蛋白-蛋白相互作用 | GNN, RF | 预测两个蛋白会不会结合 |
| 药物-靶点结合亲和力 | GNN, Transformer | 虚拟筛选潜在药物分子 |

### 4.4 表观基因组学

| 任务 | 常用方法 | 具体例子 |
|------|----------|----------|
| DNA 甲基化状态预测 | CNN, RF | 从序列特征预测 CpG 位点是否被甲基化 |
| 组蛋白修饰定位 | CNN | 从 ChIP-seq 数据中寻找修饰峰 |
| 染色质可及性预测 | CNN + ATAC-seq | 预测哪些区域是开放的调控区 |

---

## 5. 应用扩展：多组学与多模态融合

这是目前生信 ML 最热门的扩展方向 —— **不再只分析一种数据类型，而是把多种数据融合起来**。

### 5.1 什么是多组学融合

```
        基因组 (DNA 突变信息)
              +
        转录组 (RNA 表达量)
              +
        蛋白质组 (蛋白丰度)
              +
        表观组 (甲基化/染色质状态)
              +
        代谢组 (代谢物浓度)
              ↓
        联合建模 → 更全面的疾病机制理解
```

### 5.2 融合策略

| 策略 | 做法 | 优点 | 缺点 |
|------|------|------|------|
| **早期融合** | 直接把不同组学的特征拼成一个大矩阵 | 简单直观 | 不同数据尺度不同，噪声叠加 |
| **中期融合** | 各组学各自先通过一个子网络提取特征，再融合 | 平衡了各自的特异性 | 需要设计融合节点 |
| **晚期融合** | 各组学各自独立预测，最后投票/平均 | 各组学模型独立可解释 | 捕捉不到跨组学的交互 |

### 5.3 代表性方法

- **MOFA (Multi-Omics Factor Analysis)**：用贝叶斯方法找到跨组学的潜在因子，类似 PCA 的多组学版本。
- **MOGONET**：用图神经网络融合多组学数据做疾病分类。
- **scJoint**：用迁移学习把 scRNA-seq 和 scATAC-seq 数据对齐到同一空间。

### 5.4 生动例子

> 只靠基因表达（转录组）来判断癌症亚型，就像只看一个人的"说话内容"来判断他是哪里人。加上 DNA 突变（基因组）等于看了他的"身份证"，加上 DNA 甲基化（表观组）等于看了他的"成长环境"，加上蛋白质组等于看了他的"实际行为"——信息越全，判断越准。

---

## 6. 在单细胞领域的应用

单细胞测序是生信 ML/DL 应用的**绝对前沿阵地**。传统 bulk 测序是"榨一杯果汁"（混在一起测平均值），单细胞测序是"品尝每一个水果"（每个细胞的转录组单独测）。

### 6.1 单细胞数据的特点与挑战

| 特点 | 挑战 | ML 的用武之地 |
|------|------|-------------|
| 极高维度（20,000+ 基因 × 10,000+ 细胞） | 可视化困难 | 降维 (t-SNE, UMAP) |
| 极度稀疏（>90% 的表达值是 0） | 传统统计方法失效 | 专门的统计模型 + 深度学习填补 |
| 批次效应严重 | 不同实验/平台数据难以整合 | 批次校正算法 |
| 没有先验标签 | "这个细胞是什么类型？" | 无监督聚类 + 自动注释 |

### 6.2 核心任务与方法地图

```
单细胞数据分析流程
│
├── 1. 质量控制 & 标准化
│     └── scran, Seurat (传统统计)
│
├── 2. 降维与可视化
│     ├── PCA (线性降维，保留全局结构)
│     ├── t-SNE (非线性，保留局部结构)
│     └── UMAP (非线性，兼顾局部与全局，速度快) ← 当前主流
│
├── 3. 聚类 → 发现细胞类型
│     ├── K-Means, 层次聚类 (浅层方法)
│     ├── Louvain / Leiden (基于图的聚类) ← 当前主流
│     └── scVI (深度生成模型) ← 趋势
│
├── 4. 细胞类型注释
│     ├── 基于 marker gene 的人工注释 (费时费力)
│     ├── 自动注释: SingleR, CellTypist
│     └── 参考映射: Seurat CCA, scArches ← 可迁移到新数据集
│
├── 5. 差异分析
│     ├── Wilcoxon 秩和检验 (传统)
│     └── 基于模型的差异检测: MAST, scVI-latent
│
├── 6. 轨迹推断 (拟时序分析)
│     ├── Monocle3 ← 最经典
│     ├── Slingshot, PAGA
│     └── scVelo (RNA velocity) ← 能推断"方向性"
│
├── 7. 细胞间通讯
│     ├── CellPhoneDB (配体-受体分析)
│     └── NicheNet (配体→下游靶基因)
│
└── 8. 多组学整合
      ├── scATAC-seq + scRNA-seq (同一细胞)
      ├── CITE-seq (RNA + 表面蛋白)
      └── 空间转录组 (RNA + 空间位置)
```

### 6.3 单细胞中的深度学习明星模型

#### scVI (single-cell Variational Inference) —— "单细胞分析的 scikit-learn"

- **类型**：深度生成模型 (VAE)
- **功能**：降维、去噪、批次校正、差异表达、缺失值填补，一个模型全搞定。
- **为什么重要**：它把单细胞表达数据建模为一个概率生成过程，天然适合稀疏、高噪声的单细胞数据。
- **Python**：`import scvi`

#### scGPT —— "单细胞的 GPT 时刻"

- **类型**：基于 Transformer 的单细胞基础模型
- **核心思想**：像 GPT 预训练语言模型一样，在数千万个单细胞数据上预训练，然后可以在各种下游任务（细胞类型注释、基因扰动预测等）上微调。
- **生动类比**：GPT 学了整个互联网的文本后能写诗、翻译、编程。scGPT 学了海量单细胞数据后也能做注释、预测、填补等各种下游任务。
- **意义**：从"每个任务训练一个模型"走向"一个预训练大模型适应所有任务"。

#### scArches —— "单细胞数据整合的桥梁"

- **功能**：把新数据映射到已有的参考图谱上，不需要重新训练模型。
- **生动类比**：已经建立好了一张完整的"人体细胞地图"（参考图谱），新来的样本只需要在已有地图上"定位"就可以了，不用重新画地图。

### 6.4 单细胞 + 空间转录组 —— 下一个前沿

空间转录组技术在组织中保留每个细胞的物理位置信息。ML 在这里的独特贡献：

- **空间域识别** (BayesSpace, SpaGCN)：用图神经网络自动识别组织中的功能区域
- **细胞类型解卷积** (RCTD, cell2location)：空间技术往往一个 spot 包含多个细胞，需要 ML 来"拆开"
- **空间基因表达预测**：从组织切片 H&E 染色图像预测基因表达

### 6.5 一个完整的单细胞入门项目示例

```python
# 单细胞分析的最小工作示例 (使用 scanpy + scVI)

import scanpy as sc
import scvi

# 1. 读入数据
adata = sc.read_10x_h5("filtered_feature_bc_matrix.h5")

# 2. 基本预处理
sc.pp.filter_cells(adata, min_genes=200)
sc.pp.filter_genes(adata, min_cells=3)
sc.pp.normalize_total(adata)
sc.pp.log1p(adata)
sc.pp.highly_variable_genes(adata, n_top_genes=2000)

# 3. 用 scVI 做降维和批次校正
scvi.model.SCVI.setup_anndata(adata, batch_key="batch")
model = scvi.model.SCVI(adata)
model.train()
adata.obsm["X_scVI"] = model.get_latent_representation()

# 4. 聚类
sc.pp.neighbors(adata, use_rep="X_scVI")
sc.tl.umap(adata)
sc.tl.leiden(adata, resolution=0.5)

# 5. 可视化
sc.pl.umap(adata, color=["leiden", "cell_type_marker_gene"])
```

---

## 7. 深度学习在生信中的特殊地位

### 7.1 何时用传统 ML vs 深度学习？

```
传统 ML (RF, SVM, XGBoost)
├── 适用场景
│   ├── 样本量小 (< 1000)
│   ├── 特征是预定义的表格数据
│   ├── 需要解释每个特征的贡献
│   └── 快速建立基线模型
│
└── 不适用
    ├── 需要从序列/图像中自动提取特征
    └── 数据量巨大但有复杂模式

深度学习 (CNN, Transformer, GNN)
├── 适用场景
│   ├── 序列数据 (DNA, RNA, 蛋白质)
│   ├── 图像数据 (病理切片)
│   ├── 有大量标记数据或有预训练模型可以迁移
│   └── 端到端学习 (从原始数据直接到预测结果)
│
└── 不适用
    ├── 样本很少且无法用迁移学习
    └── 需要严格的可解释性
```

### 7.2 生信领域的代表性深度学习模型

| 模型 | 年份 | 领域 | 核心架构 | 意义 |
|------|------|------|----------|------|
| DeepVariant | 2017 | 变异检测 | CNN | 把测序读段 pileup 转成图像，用 CNN 找变异 |
| DeepBind | 2015 | 蛋白-DNA 结合 | CNN | 从序列预测转录因子结合位点 |
| AlphaFold2 | 2021 | 蛋白质结构 | Transformer | 从序列预测 3D 结构，精度达实验级别 |
| Enformer | 2021 | 基因表达预测 | Transformer | 从 200K bp 的 DNA 序列预测基因表达 |
| ESM-2 | 2022 | 蛋白质语言模型 | Transformer | 蛋白质的"GPT"，可用于结构、功能预测 |
| scGPT | 2023 | 单细胞 | Transformer | 单细胞基础模型，多任务适应 |
| Geneformer | 2023 | 基因网络 | Transformer | 在大量单细胞数据上预训练的基因上下文模型 |

---

## 8. 初学者学习路线图

### 阶段一：基础准备（2-4 周）

**目标**：能舒适地用 Python 处理数据，理解基本 ML 概念。

```
第 1 周：Python 基础
├── NumPy (矩阵运算)
├── Pandas (表格数据处理)
└── Matplotlib / Seaborn (数据可视化)

第 2 周：统计学基础
├── 概率分布、假设检验
├── 线性代数：矩阵乘法、特征值 → 理解 PCA 的基础
└── 微积分：导数 → 理解梯度下降的基础

第 3-4 周：ML 基础概念
├── 偏差-方差权衡
├── 交叉验证
├── 过拟合 vs 欠拟合
└── 评估指标（AUC, F1, RMSE 等）
```

**推荐资源**：
- 吴恩达《Machine Learning》Coursera 课程
- 《统计学习方法》(李航) —— 适合数学基础较好的读者
- Kaggle 上的 "Intro to Machine Learning" 免费微课程

### 阶段二：传统 ML 实战（3-4 周）

**目标**：能用 scikit-learn 搭建生信分类/回归模型。

```
动手项目建议（按难度递进）：

1. 鸢尾花分类 (经典入门，理解 fit/predict 流程)
2. 乳腺癌基因表达分类 (TCGA 数据, 使用 RF/SVM)
3. 药物敏感性预测 (GDSC 数据, 使用 XGBoost)
4. 蛋白质亚细胞定位预测 (序列特征 + RF/XGBoost)
```

**关键技能**：
- 特征工程：如何从生物序列中提取数值特征
- 模型选择：什么时候用哪个算法
- 超参数调优：GridSearchCV, RandomizedSearchCV
- 模型评估：在生物数据上如何正确做交叉验证

### 阶段三：生信 ML 深入（3-4 周）

**目标**：能独立完成一篇生信 ML 论文的分析流程。

```
核心包学习：

├── Scanpy (单细胞分析)
├── scikit-learn (传统 ML)
├── XGBoost / LightGBM (表格数据王者)
├── DESeq2 (通过 rpy2 调用, 差异分析)
└── bioconductor 生态
```

**代表性项目**：
- TCGA 癌症亚型分类与生存分析
- Drug response prediction (GDSC + CCLE)
- 基因调控网络推断 (GENIE3, GRNBoost)

### 阶段四：深度学习入门（4-6 周）

**目标**：能用 PyTorch 搭建简单的 DL 模型处理生信问题。

```
第 1-2 周：PyTorch 基础
├── Tensor, Autograd
├── nn.Module, DataLoader
└── 训练循环 (train/val/test loop)

第 3-4 周：生信 DL 应用
├── 用 CNN 做 DNA 序列分类 (如启动子预测)
├── 用 LSTM/Transformer 预测蛋白质功能
└── 用 scVI 做单细胞数据整合

第 5-6 周：进阶主题
├── 迁移学习：用预训练模型提取特征
├── 图神经网络 (GNN)：蛋白互作网络分析
└── 可解释性：SHAP, Integrated Gradients
```

### 阶段五：持续进阶

- **阅读论文**：保持每周 1-2 篇顶会/顶刊论文
  - 计算方法：*Nature Methods*, *Nature Biotechnology*, *Genome Biology*, *Bioinformatics*
  - ML 会议：*NeurIPS*, *ICML*, *ICLR* 中的计算生物 track
- **复现论文**：挑选有开源代码的论文，从头跑通
- **参与竞赛**：Kaggle 生物相关竞赛、CAFA (蛋白功能预测)、CAGI
- **建立自己的项目**：找一个小问题，从数据到模型完整做一遍

---

## 9. 常用工具与资源

### 9.1 Python 核心生态

```
数据科学基础:
├── NumPy, Pandas, SciPy
│
机器学习:
├── scikit-learn        (传统 ML 全家桶)
├── XGBoost, LightGBM   (梯度提升)
│
深度学习:
├── PyTorch             (学术界主流，推荐初学者选这个)
├── TensorFlow / Keras  (工业界多用)
│
生信专用:
├── Scanpy              (单细胞分析必备)
├── scVI                (单细胞深度生成模型)
├── Biopython           (序列处理)
├── anndata             (单细胞数据格式)
│
可视化:
├── Matplotlib, Seaborn (基础绑图)
├── Plotly              (交互式图表)
└── scanpy.pl           (单细胞绑图)
```

### 9.2 R 语言生态（生信传统强项）

```
├── Seurat               (单细胞分析)
├── DESeq2 / edgeR       (差异表达)
├── caret / tidymodels   (ML 框架)
├── Monocle3             (拟时序分析)
└── WGCNA                (加权基因共表达网络)
```

### 9.3 数据资源

| 数据库 | 内容 | 网址 |
|--------|------|------|
| TCGA | 33 种癌症的多组学数据 | portal.gdc.cancer.gov |
| GEO | 最大的公共基因表达数据库 | ncbi.nlm.nih.gov/geo |
| Single Cell Portal | 单细胞数据集汇总 | singlecell.broadinstitute.org |
| The Human Cell Atlas | 人体细胞参考图谱 | humancellatlas.org |
| GDSC | 药物敏感性基因组数据 | cancerrxgene.org |
| UniProt | 蛋白质功能注释 | uniprot.org |
| PDB | 蛋白质 3D 结构 | rcsb.org |

### 9.4 学习资源

**在线课程**（按推荐顺序）：
1. **吴恩达 Machine Learning (Coursera)** —— 没有比这更适合零基础入门的 ML 课
2. **Fast.ai Practical Deep Learning** —— 自上而下的学习方法，先会用再学原理
3. **Stanford CS224N (NLP with DL)** —— 理解 Transformer 原理
4. **哈佛 STAT115/215** —— 专门面向生物信息学的计算课程

**书籍**：
- 《An Introduction to Statistical Learning》(ISLR) —— ML 统计学圣经，有免费 PDF
- 《Deep Learning》(Goodfellow et al.) —— 深度学习"花书"，适合理论深造
- 《Bioinformatics Data Skills》(Vince Buffalo) —— 生信数据处理实践
- 《Deep Learning for the Life Sciences》—— 深度学习在生命科学中的应用

---

## 10. 常见误区与避坑指南

### 误区一："模型越复杂越好"

**现实**：在样本量小的生信数据上，XGBoost 经常比深度学习效果好。不要一上来就套用 Transformer，从简单的模型开始建立基线。

> **金句**：先跑通一个 Logistic Regression 基线，再考虑加复杂度。

### 误区二："特征越多越好"

**现实**：2 万个基因中可能只有几百个与你的问题相关。特征选择（或正则化）不仅能提升性能，还能提供生物学 insight。

### 误区三："训练集准确率高就是好模型"

**现实**：在生信中，由于数据量小、批次效应大，训练集表现和独立测试集表现可以天差地别。**必须留出独立的测试集，或者使用交叉验证。** 最好的验证是：用来自不同机构/平台的外部数据集做独立验证。

### 误区四："t-SNE 图上的距离有意义"

**现实**：t-SNE/UMAP 图中，簇与簇之间的距离、簇的大小都没有生物学意义。这些图只能告诉你"哪些细胞在一起"，不能告诉你"这一群比那一群更相似"。

### 误区五："深度学习不需要特征工程"

**现实**：在生信中，好的特征工程（如从序列中提取 k-mer 频率、理化性质等）仍然是重要的。深度学习可以减少手动特征工程，但不能完全替代领域知识。

### 误区六："学完课程就能做研究了"

**现实**：最好的学习方式是 **project-driven（项目驱动）**。找一个你真正感兴趣的生信问题，边做边学。当你卡住的时候，那个卡点就是你需要学的下一个知识点。

### 误区七："只学 Python / R 就够了"

**现实**：生信 ML 通常需要 **Python + R 双修**。R 在统计分析和生物信息学下游分析（差异分析、富集分析、可视化）方面有深厚积淀，Python 在 ML/DL 方面优势明显。建议：数据预处理和统计测试用 R，ML/DL 建模用 Python，两者通过 `reticulate` 或 `rpy2` 互相调用。

---

## 总结：一张全景导航图

```
生信 ML/DL 学习全景

        Python 基础          统计学基础        生物学基础
            │                   │                  │
            └───────────────────┼──────────────────┘
                                ▼
                    ┌───────────────────┐
                    │   传统 ML 入门     │
                    │  LR, RF, SVM...   │
                    └───────┬───────────┘
                            │
              ┌─────────────┼─────────────┐
              ▼             ▼             ▼
        ┌──────────┐ ┌──────────┐ ┌──────────────┐
        │ 基因组学  │ │ 转录组学 │ │ 蛋白质组学    │
        │ ML 应用   │ │ ML 应用  │ │ ML 应用       │
        └─────┬────┘ └─────┬────┘ └──────┬───────┘
              │             │             │
              └─────────────┼─────────────┘
                            │
                            ▼
                    ┌───────────────────┐
                    │   深度学习入门     │
                    │ CNN/RNN/Transformer│
                    └───────┬───────────┘
                            │
              ┌─────────────┼─────────────┐
              ▼             ▼             ▼
        ┌──────────┐ ┌──────────┐ ┌──────────────┐
        │ 单细胞    │ │ 多组学   │ │ AlphaFold /  │
        │ scVI/scGPT│ │ MOFA/融合│ │ ESM / 结构   │
        └──────────┘ └──────────┘ └──────────────┘
                            │
                            ▼
                    ┌───────────────────┐
                    │  前沿研究 & 创新   │
                    │ 读论文→复现→自己做 │
                    └───────────────────┘
```

---

> **最后的话**：生信机器学习是一个交叉学科，你不需要在每个方向上都是专家。关键是在**生物学问题**和**计算方法**之间找到自己的定位。有些人擅长提好的生物学问题然后用 ML 解决，有些人擅长开发新的 ML 方法来解决生物学难题。两条路都很有价值。**Start small, stay curious, and let the data guide you.**

---

*文档生成于 2026-05-23 | 祝你入门顺利！*
