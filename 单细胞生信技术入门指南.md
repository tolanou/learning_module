# 单细胞生物信息学技术入门指南

> 适用对象：干细胞与再生医学方向研究生 | 从数据处理到前沿技术全景

---

## 目录

1. [单细胞测序技术概述](#1-单细胞测序技术概述)
2. [实验流程与数据产生](#2-实验流程与数据产生)
3. [数据处理基础流程](#3-数据处理基础流程)
4. [核心分析模块详解](#4-核心分析模块详解)
5. [高级分析方法](#5-高级分析方法)
6. [前沿技术拓展](#6-前沿技术拓展)
7. [多技术联合应用场景](#7-多技术联合应用场景)
8. [常用工具与资源汇总](#8-常用工具与资源汇总)
9. [推荐学习路径](#9-推荐学习路径)

---

## 1. 单细胞测序技术概述

### 1.1 为什么需要单细胞测序？

传统Bulk RNA-seq测的是**一群细胞的平均基因表达**，就像把一篮子水果榨成汁——你无法知道苹果和橙子各自的味道。单细胞测序（scRNA-seq）则是**逐一品尝每个水果**，能揭示：

- 组织中存在哪些**细胞亚群**（细胞异质性）
- 细胞状态的**动态变化**（发育、衰老、疾病过程中的转变）
- **稀有细胞类型**的发现（如成体干细胞、循环肿瘤细胞）
- 细胞间的**谱系关系**（谁分化成了谁）

### 1.2 主流技术平台对比

| 平台 | 通量 | 灵敏度 | 全长覆盖 | 适用场景 |
|------|------|--------|----------|----------|
| **10x Genomics Chromium** | 高（500-10,000细胞/样本） | 中 | 3'/5'端 | 图谱构建、大规模分群 |
| **Smart-seq2** | 低（96-384细胞/板） | 高 | 全长 | 深度表征、可变剪切 |
| **Drop-seq** | 极高 | 低 | 3'端 | 超大规模图谱 |
| **Parse Biosciences** | 高（可固定样本） | 中 | 3'端 | 临床样本、多时间点 |
| **BD Rhapsody** | 高 | 中 | 3'端 | 孔板法替代方案 |

> **入门首选 10x Genomics**——性价比最高、生态最完善、教程最丰富

### 1.3 数据产出概览（以10x为例）

```
一个样本（1,000个细胞）：
├── FASTQ文件（原始读序）           ~2-5 GB
├── 基因表达矩阵（UMI counts）      ~50-200 MB
├── 每个细胞平均检测基因数          1,000-3,000
├── 每个细胞平均UMI数               2,000-10,000
└── 测序饱和度                      30%-70%（视目标而定）
```

---

## 2. 实验流程与数据产生

### 2.1 湿实验关键步骤

```
样本制备 → 单细胞悬液 → 细胞计数与活性检测
   → 微流控捕获（油包水/微孔）→ 逆转录与扩增
   → 文库构建 → 测序（Illumina平台为主）
```

**关键质控点：**
- 细胞活性 > 85%（死细胞过多会产生背景噪音）
- 细胞浓度 700-1,200 cells/μL（10x平台建议）
- 无明显的细胞碎片/聚集体
- 对于组织样本：酶消化条件需要严格控制，避免诱导应激基因表达

### 2.2 从FASTQ到表达矩阵

```bash
# Cell Ranger 标准流程（以10x小鼠数据为例）
# Step 1: 下载参考基因组
wget https://cf.10xgenomics.com/supp/cell-exp/refdata-gex-mm10-2020-A.tar.gz
tar -xzf refdata-gex-mm10-2020-A.tar.gz

# Step 2: 运行cellranger count
cellranger count \
  --id=sample01 \
  --transcriptome=refdata-gex-mm10-2020-A \
  --fastqs=./fastq/ \
  --sample=sample01 \
  --expect-cells=5000 \
  --localcores=16 \
  --localmem=64
```

**输出关键文件：**
- `filtered_feature_bc_matrix.h5` → 过滤后的细胞×基因UMI矩阵
- `web_summary.html` → 质控总览（必看！）
- `metrics_summary.csv` → 各项统计指标

---

## 3. 数据处理基础流程

### 3.1 分析环境搭建

推荐使用 **Scanpy**（Python）或 **Seurat**（R）：

```python
# Python 环境
pip install scanpy scvelo scvi-tools cellrank squidpy

# 或使用 R 环境（Seurat 生态）
# install.packages("Seurat")
# BiocManager::install(c("scran", "scater", "scuttle"))
```

```python
import scanpy as sc
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns

# Scanpy 全局设置
sc.settings.verbosity = 3             # 输出详细日志
sc.settings.set_figure_params(dpi=120, facecolor='white')
sc.logging.print_header()
```

### 3.2 加载数据

```python
# 从10x Cell Ranger输出加载
adata = sc.read_10x_h5('filtered_feature_bc_matrix.h5')
adata.var_names_make_unique()

# 或从文本文件加载
# adata = sc.read_text('matrix.mtx')

print(f"原始数据: {adata.n_obs} 个Barcode, {adata.n_vars} 个基因")
```

### 3.3 质控过滤（QC）

这是**最关键的步骤**——质控做得不好，后续分析全白做。

```python
# 计算QC指标
adata.var['mt'] = adata.var_names.str.startswith('MT-')    # 人线粒体
# adata.var['mt'] = adata.var_names.str.startswith('mt-')  # 小鼠线粒体
adata.var['ribo'] = adata.var_names.str.startswith(('RPS', 'RPL'))  # 核糖体

sc.pp.calculate_qc_metrics(adata, qc_vars=['mt', 'ribo'], percent_top=None, log1p=False)

# 可视化QC
fig, axes = plt.subplots(1, 3, figsize=(14, 4))
sc.pl.violin(adata, 'n_genes_by_counts', ax=axes[0])
sc.pl.violin(adata, 'total_counts', ax=axes[1])
sc.pl.violin(adata, 'pct_counts_mt', ax=axes[2])

# 过滤标准（需根据具体数据集调整！）
sc.pp.filter_cells(adata, min_genes=200)          # 至少检测到200个基因
sc.pp.filter_genes(adata, min_cells=3)             # 至少在3个细胞中表达

# 过滤双细胞（doublet）和死细胞
adata = adata[adata.obs['n_genes_by_counts'] < 6000, :]
adata = adata[adata.obs['pct_counts_mt'] < 20, :]

print(f"QC后: {adata.n_obs} 个细胞, {adata.n_vars} 个基因")
```

**QC标准参考值：**
| 指标 | 太低意味着 | 太高意味着 |
|------|-----------|-----------|
| n_genes（检测基因数） | 空液滴、死细胞 | Doublet |
| total_counts（总UMI） | 空液滴、碎片 | Doublet |
| pct_mt（线粒体比例） | - | 死细胞/受损细胞（>15-20%） |
| pct_ribo（核糖体比例） | - | 某些活跃分泌细胞也可能高 |

### 3.4 标准化与高变基因选择

```python
# 保存原始counts用于后续差异分析
adata.raw = adata.copy()

# 文库大小标准化到10,000
sc.pp.normalize_total(adata, target_sum=1e4)

# 对数转换
sc.pp.log1p(adata)

# 选择高变基因（HVGs）
sc.pp.highly_variable_genes(adata, n_top_genes=2000)
sc.pl.highly_variable_genes(adata)

# 仅保留高变基因用于降维
adata = adata[:, adata.var.highly_variable]
```

### 3.5 降维与聚类

```python
# 缩放到单位方差
sc.pp.scale(adata, max_value=10)

# PCA降维
sc.tl.pca(adata, svd_solver='arpack')
sc.pl.pca_variance_ratio(adata, n_pcs=50)  # 选择PC数目的elbow plot

# 计算邻域图（决定聚类质量的关键参数）
sc.pp.neighbors(adata, n_neighbors=15, n_pcs=30)

# UMAP可视化
sc.tl.umap(adata)
sc.pl.umap(adata, color=['n_genes_by_counts', 'pct_counts_mt'])

# Leiden聚类（常用分辨率0.4-1.2）
sc.tl.leiden(adata, resolution=0.8)
sc.pl.umap(adata, color='leiden', legend_loc='on data')
```

---

## 4. 核心分析模块详解

### 4.1 细胞类型注释

这是承上启下的**核心环节**，注释质量决定后续分析的可靠性。

**方法一：已知marker自动注释**

```python
# 手动marker注释示例（人和小鼠通用标记）
marker_dict = {
    'HSC': ['CD34', 'KIT', 'CD38'],
    'T_cell': ['CD3D', 'CD3E', 'CD2'],
    'B_cell': ['CD79A', 'MS4A1', 'CD19'],
    'NK_cell': ['NKG7', 'GNLY', 'KLRD1'],
    'Monocyte': ['CD14', 'FCGR3A', 'CSF1R'],
    'Macrophage': ['CD68', 'CD163'],
    'Fibroblast': ['COL1A1', 'DCN', 'LUM'],
    'Endothelial': ['PECAM1', 'CDH5', 'VWF'],
    'Epithelial': ['EPCAM', 'KRT8', 'KRT18'],
    'Skeletal_muscle': ['MYOD1', 'MYOG', 'ACTA1'],
    'Neuron': ['TUBB3', 'MAP2', 'SYN1'],
    'Oligodendrocyte': ['MBP', 'PLP1', 'MOG'],
    'Pericyte': ['RGS5', 'PDGFRB'],
    'Smooth_muscle': ['ACTA2', 'MYH11', 'TAGLN'],
}

# 绘制marker小提琴图验证
sc.pl.stacked_violin(adata, marker_dict['HSC'], groupby='leiden', rotation=90)
sc.pl.dotplot(adata, marker_dict['T_cell'], groupby='leiden', dendrogram=True)
```

**方法二：自动注释工具**

```python
# CellTypist（基于逻辑回归，速度快、准确率高）
# pip install celltypist
import celltypist
predictions = celltypist.annotate(adata, model='Human_Lung_Atlas.pkl', gene_file='.h5ad')
adata.obs['celltypist_label'] = predictions.predicted_labels['majority_voting']

# SingleR（R包，基于相关性匹配）
# library(SingleR)
# pred <- SingleR(test=as.SingleCellExperiment(adata), ref=ref_data, labels=ref_data$label)
```

### 4.2 差异表达分析

```python
# 每个cluster相对于其他所有cluster的差异基因
sc.tl.rank_genes_groups(adata, 'leiden', method='wilcoxon')
sc.pl.rank_genes_groups(adata, n_genes=15, sharey=False)

# 提取某个cluster的top10 marker
df_markers = sc.get.rank_genes_groups_df(adata, group='0')
top10 = df_markers.head(10)

# 条件间差异（如：治疗 vs 对照）
sc.tl.rank_genes_groups(adata, 'condition', groups=['treated'], 
                         reference='control', method='wilcoxon')
```

**Marker基因筛选标准：**
- `log2FC > 0.5`（表达倍数变化足够大）
- `p_val_adj < 0.05`（统计显著）
- `pct.1 > 0.25`（在目标组至少25%细胞中表达）
- 生物学意义优先于统计显著性

### 4.3 功能富集分析

```python
# GO/KEGG 通路富集
import gseapy as gp

gene_list = df_markers.names[:100].tolist()
enr = gp.enrichr(gene_list=gene_list,
                 gene_sets=['KEGG_2021_Human', 'GO_Biological_Process_2021'],
                 organism='Human',
                 outdir='enrichr_results')

# 可视化
sc.pl.umap(adata, color=['TOP2A', 'MKI67', 'PCNA'])  # 细胞周期基因
```

---

## 5. 高级分析方法

### 5.1 拟时序分析（Trajectory Inference）

理解细胞状态转变的核心工具——干细胞分化、衰老过程的动力学。

```python
# RNA Velocity（推荐：scVelo）
import scvelo as scv

# 需要未过滤的spliced/unspliced计数
scv.pp.filter_and_normalize(adata)
scv.pp.moments(adata)
scv.tl.velocity(adata, mode='stochastic')
scv.tl.velocity_graph(adata)
scv.pl.velocity_embedding_stream(adata, basis='umap')

# PAGA 图抽象
sc.tl.paga(adata, groups='leiden')
sc.pl.paga(adata, plot=True)

# Monocle3 / Palantir 伪时间分析
# Palantir 适合捕捉分化命运的"分叉"点
```

### 5.2 细胞通讯分析

```python
# CellPhoneDB（配体-受体互作分析，命令行运行）
# cellphonedb method statistical_analysis meta.txt counts.txt

# LIANA（CellPhoneDB + NicheNet + SingleCellSignalR 统一框架）
# pip install liana
import liana as li
li.test.rank_aggregate(adata, groupby='cell_type', expr_prop=0.1)

# 关键互作对可视化
li.pl.dotplot(adata=adata, source_groups=['Fibroblast'], 
              target_groups=['Epithelial'], top_n=15)
```

### 5.3 转录因子调控网络（SCENIC）

```python
# pySCENIC：从共表达网络推断TF调控活性
# pyscenic grn \
#   expr_mat.tsv \
#   hs_hgnc_curated_tfs.txt \
#   -o adj.tsv
#
# pyscenic ctx adj.tsv \
#   databases/hg38__refseq-r80__*.feather \
#   --annotations_fname motifs-v9-nr.hgnc-m0.001-o0.0.tbl \
#   -o regulons.csv
#
# pyscenic aucell expr_mat.tsv regulons.csv -o auc_mtx.csv

# 或使用简化版：DoRothEA
import decoupler as dc
dc.run_dorothea(adata, organism='human')
```

### 5.4 拷贝数变异推断

```python
# inferCNV：从scRNA-seq数据推断基因组拷贝数变异
# 用于区分肿瘤细胞 vs 正常细胞
# R: library(infercnv)
# infercnv::run(infercnv_obj, cutoff=0.1, 
#               cluster_by_groups=TRUE, denoise=TRUE, HMM=FALSE)
```

---

## 6. 前沿技术拓展

### 6.1 单细胞多组学

| 技术 | 检测内容 | 代表平台 | 应用方向 |
|------|----------|----------|----------|
| **CITE-seq / AbSeq** | RNA + 表面蛋白 | 10x / BD | 免疫表型深度刻画 |
| **scATAC-seq** | 染色质开放性 | 10x Multiome | 表观调控网络 |
| **scMethyl-seq** | DNA甲基化 | 自定义 | 表观遗传异质性 |
| **scHi-C** | 染色质三维构象 | 自定义 | 基因组空间结构 |
| **CUT&Tag / scCUT&Tag** | 组蛋白修饰 | 10x / 自定义 | 表观修饰单细胞分辨 |

### 6.2 10x Multiome（RNA + ATAC 同时测序）

```python
# Signac + Seurat 联合分析
# R示例：
# atac <- Read10X_h5("atac_filtered_peak_bc_matrix.h5")
# rna <- Read10X_h5("rna_filtered_feature_bc_matrix.h5")
# obj <- CreateSeuratObject(counts = rna)
# obj[["ATAC"]] <- CreateChromatinAssay(counts = atac)
#
# # WNN分析：加权整合RNA和ATAC模态
# obj <- FindMultiModalNeighbors(obj, reduction.list = list("pca", "lsi"),
#                                 dims.list = list(1:30, 2:30))
# obj <- RunUMAP(obj, nn.name = "weighted.nn")

# Python 端（Muon + scVI-tools）
import muon as mu
mdata = mu.read_10x_h5("filtered_feature_bc_matrix.h5")
# 分别处理 RNA 和 ATAC 模态后联合嵌入
```

### 6.3 空间转录组学（Spatial Transcriptomics）

这是目前**最热的方向**——保留空间位置信息的转录组。

| 技术 | 分辨率 | 覆盖范围 | 特点 |
|------|--------|----------|------|
| **10x Visium** | 55μm（spot级别） | 全组织切片 | 成熟生态，适合图谱 |
| **Visium HD** | 2μm（接近单细胞） | 全组织切片 | 2024年推出，高分辨率 |
| **MERFISH** | 单细胞 | 亚细胞 | 靶向基因，高灵敏度 |
| **Stereo-seq** | 500nm | 厘米级 | 华大基因，超高分辨 |
| **Slide-seq V2** | 10μm | 3mm直径 | bead级别分辨率 |
| **Xenium** | 亚细胞 | 可定制区域 | 10x原位测序平台 |

```python
# Squidpy 空间转录组分析
import squidpy as sq

# 空间邻居图
sq.gr.spatial_neighbors(adata, coord_type='grid')

# 空间自相关分析（Moran's I）
sq.gr.spatial_autocorr(adata, genes=['TOP2A', 'MKI67'], mode='moran')

# 邻域富集分析
sq.gr.nhood_enrichment(adata, cluster_key='cell_type')
sq.pl.nhood_enrichment(adata, cluster_key='cell_type')

# Ligand-Receptor 空间共定位
sq.gr.ligrec(adata, n_perms=1000)
```

### 6.4 单细胞 CRISPR 筛选（Perturb-seq）

将CRISPR扰动与scRNA-seq读出的结合：

```
实验设计：pooled sgRNA文库 → 慢病毒转导 → 单细胞分选+测序
分析方法：MIMOSCA、scMAGeCK、mixscape
应用：高通量功能基因组学、药物靶点筛选
```

### 6.5 类器官单细胞多组学

- 肠道/脑/肿瘤类器官的scRNA-seq图谱构建
- 药物处理的单细胞响应异质性分析
- 与体内对应组织进行比较，评估类器官保真度

---

## 7. 多技术联合应用场景

### 7.1 场景一：干细胞分化谱系追踪

```
整合方案：
  scRNA-seq（转录状态）
+ scATAC-seq（染色质可及性动态）
+ RNA Velocity（分化方向预测）
+ 谱系条形码（Lineage Tracing / ScarTrace）
→ 构建完整的干细胞分化树
```

**代表性研究工具：**
- CellTag / ScarTrace：可诱导条形码标记克隆
- LINNAEUS / intMEMOIR：基于CRISPR的谱系记录
- **应用**：造血干细胞→各谱系分化的完整路径图

### 7.2 场景二：衰老微环境解析

```
整合方案：
  scRNA-seq（细胞异质性）
+ Spatial Transcriptomics（空间微环境）
+ scATAC-seq（表观衰老特征）
+ scProteomics（蛋白层面验证）
→ 解析衰老微环境中细胞互作网络
```

**关注点：**
- 衰老组织中SASP（衰老相关分泌表型）的空间梯度
- 干细胞niche随衰老的组成和功能变化
- 免疫细胞在衰老组织中的浸润和功能改变

### 7.3 场景三：疾病机制与靶点发现

```
整合方案：
  scRNA-seq（疾病 vs 正常对比）
+ Perturb-seq（高通量基因敲除/抑制筛选）
+ CellPhoneDB/LIANA（异常细胞通讯识别）
+ Spatial（关键靶点的空间验证）
→ 发现新的治疗靶点和生物标志物
```

### 7.4 场景四：跨物种/跨器官比较

```
整合方案：
  多物种scRNA-seq数据
+ 同源基因映射
+ 跨物种整合（scVI / Harmony / SATURN）
→ 发现进化保守的细胞类型和调控程序
```

### 7.5 实际联合分析框架图

```
原始组织/样本
    │
    ├──→ 单细胞悬液 ──→ 10x scRNA-seq ──→ 细胞图谱 + 差异基因
    │         │
    │         ├──→ 10x Multiome ──→ RNA + ATAC 联合分析
    │         │
    │         └──→ CITE-seq ──→ 转录组 + 表面蛋白双重验证
    │
    ├──→ 冷冻切片 ──→ Visium空间转录组 ──→ 空间定位 + 微环境
    │
    └──→ 分离细胞核 ──→ scATAC-seq ──→ 表观调控 + TF活性推断
    
    所有结果整合 → Louvain/Leiden分类 → 细胞通讯 → 功能验证
```

---

## 8. 常用工具与资源汇总

### 8.1 核心分析工具

| 工具 | 语言 | 用途 | 学习曲线 |
|------|------|------|----------|
| **Scanpy** | Python | 一站式单细胞分析 | ⭐⭐ |
| **Seurat** | R | 最广泛使用的单细胞工具包 | ⭐⭐ |
| **scVelo** | Python | RNA速率分析 | ⭐⭐⭐ |
| **CellPhoneDB** | Python | 配体-受体互作 | ⭐⭐ |
| **pySCENIC** | Python | 转录因子网络推断 | ⭐⭐⭐⭐ |
| **Squidpy** | Python | 空间转录组分析 | ⭐⭐⭐ |
| **celltypist** | Python | 自动细胞注释 | ⭐ |
| **Harmony** | R/Python | 批次效应校正 | ⭐⭐ |
| **scVI-tools** | Python | 深度生成模型整合 | ⭐⭐⭐ |
| **monocle3** | R | 拟时序分析 | ⭐⭐⭐ |

### 8.2 公共数据库

| 数据库 | 网址 | 内容 |
|--------|------|------|
| **Human Cell Atlas** | https://www.humancellatlas.org | 人类全身细胞图谱 |
| **Tabula Sapiens / Muris** | https://tabula-sapiens.science | 人/小鼠多器官图谱 |
| **PanglaoDB** | https://panglaodb.se | 人和小鼠单细胞marker数据库 |
| **CellMarker 2.0** | http://bio-bigdata.hrbmu.edu.cn/CellMarker | 细胞类型marker数据库 |
| **Cellxgene** | https://cellxgene.cziscience.com | CZI交互式单细胞数据探索 |
| **TISSUES** | https://tissues.jensenlab.org | 组织特异性基因表达 |
| **DisGeNET** | https://www.disgenet.org | 基因-疾病关联 |
| **Gene Ontology** | https://geneontology.org | GO功能注释 |
| **KEGG** | https://www.genome.jp/kegg | 通路数据库 |
| **STRING** | https://string-db.org | 蛋白质互作网络 |

### 8.3 中国学者常用平台

| 平台 | 说明 |
|------|------|
| **国家基因库（CNGB）** | https://db.cngb.org | 华大基因支撑的公共数据库 |
| **GSA（Genome Sequence Archive）** | https://ngdc.cncb.ac.cn/gsa | 中国数据提交平台 |
| **Cell Taxonomy** | https://ngdc.cncb.ac.cn/celltaxonomy | 中科院细胞分类数据库 |
| **CST fathers** | https://www.cstfathers.org | 中山大学单细胞数据库 |

### 8.4 必读综述

搜索以下关键词查找最新综述：
- "Single-cell genomics technologies and applications" (Nature Reviews Genetics)
- "Best practices for single-cell analysis across modalities" (Nature Reviews Genetics, 2023)
- "Single-cell multi-omics: technologies and applications" (Nature Methods)
- "Spatial transcriptomics coming of age" (Nature Reviews Genetics, 2024)
- "A practical guide to single-cell RNA-sequencing for biomedical research" (Genome Medicine)

---

## 9. 推荐学习路径

### 第一阶段：基础入门（2-4周）

```
Week 1: 理解scRNA-seq原理和实验流程
  → 安装 Scanpy/Seurat
  → 跑通教程数据（pbmc3k）
  → 理解QC、标准化、降维、聚类的每一步在做什么

Week 2: 细胞注释和差异分析
  → 掌握marker基因鉴定方法
  → 学会自动/手动细胞注释
  → 能完成不同条件下的差异表达分析

Week 3-4: 完成一个小项目
  → 从GEO下载一篇论文的数据
  → 复现其核心分析流程
  → 做出UMAP图、marker dotplot、差异基因热图
```

### 第二阶段：进阶分析（4-8周）

```
Week 5-6: 拟时序与RNA速率
  → 掌握scVelo / Monocle3
  → 理解分化/状态转变的数学表示

Week 7-8: 细胞通讯与调控网络
  → CellPhoneDB / LIANA 配体-受体分析
  → pySCENIC 转录因子调控网络推断
```

### 第三阶段：多组学整合（持续学习）

```
长期:
  → 学习 scATAC-seq 分析（Signac / ArchR）
  → 学习空间转录组分析（Squidpy / Giotto）
  → 了解深度学习方法（scVI / scGPT / Geneformer）
  → 关注预训练基础模型在单细胞中的应用
```

### 入门练习数据集推荐

1. **pbmc3k**（Scanpy/Seurat内置）—— 外周血单个核细胞，3k细胞
2. **Tabula Muris** —— 小鼠多器官图谱，经典参考
3. **10x官方数据集** —— https://www.10xgenomics.com/datasets
4. **GEO** —— 搜索你感兴趣的组织（如 "skeletal muscle scRNA-seq"）

---

## 附录：常用代码速查

### Scanpy 标准分析流程（一键运行核心步骤）

```python
import scanpy as sc

# === 加载与QC ===
adata = sc.read_10x_h5('filtered_feature_bc_matrix.h5')
adata.var_names_make_unique()
adata.var['mt'] = adata.var_names.str.startswith('MT-')
sc.pp.calculate_qc_metrics(adata, qc_vars=['mt'], percent_top=None, log1p=False)
sc.pp.filter_cells(adata, min_genes=200)
sc.pp.filter_genes(adata, min_cells=3)
adata = adata[adata.obs['pct_counts_mt'] < 20, :]

# === 标准化与降维 ===
adata.raw = adata.copy()
sc.pp.normalize_total(adata, target_sum=1e4)
sc.pp.log1p(adata)
sc.pp.highly_variable_genes(adata, n_top_genes=2000)
adata = adata[:, adata.var.highly_variable]
sc.pp.scale(adata, max_value=10)
sc.tl.pca(adata, svd_solver='arpack')
sc.pp.neighbors(adata, n_neighbors=15, n_pcs=30)
sc.tl.umap(adata)
sc.tl.leiden(adata, resolution=0.8)

# === 差异分析 ===
sc.tl.rank_genes_groups(adata, 'leiden', method='wilcoxon')

# === 保存结果 ===
adata.write('analysis_result.h5ad')
```

### 批次效应校正（多数据集整合）

```python
# Harmony
import harmonypy
sc.external.pp.harmony_integrate(adata, 'batch')

# scVI（深度生成模型，推荐）
import scvi
scvi.model.SCVI.setup_anndata(adata, batch_key='batch')
model = scvi.model.SCVI(adata)
model.train()
adata.obsm['X_scVI'] = model.get_latent_representation()
```

---

> **最后建议**：单细胞生信入门最快的路径是**边做边学**。下载一个公共数据，从头到尾跑一遍分析流程，遇到报错解决问题，远比只看教程高效。遇到问题优先查 Scanpy/Seurat 官方文档和 GitHub Issues，其次是 Biostars 和 Stack Overflow。
>
> 张宏波老师课题组偏重干细胞与线粒体生物学，建议你在学习生信的同时，多思考如何将单细胞技术应用到线粒体功能、干细胞命运决定的科学问题中——**技术与生物学的结合点才是最有价值的地方**。
